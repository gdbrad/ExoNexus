[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ExoNexus"
version = "0.1.0"
description = "ExoNexus LQCD analysis framework"
authors = [{name="gdbrad"}]
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
